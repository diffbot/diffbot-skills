__pycache__/
*.pyc
*.pyo
.cursor/
dist/
.DS_Store
# Cowork bundling tooling and its vendored deps live on the `cowork` branch.
# Ignored here so a rebase or stray build never reintroduces them to main.
vendor/
