# Anthropic listing — `claude-plugins-official`

Hand this folder to the Anthropic / partner curator contact (not the public
community submission form). It's everything needed to add `diffbot` to the
`claude-plugins-official` marketplace.

## What to do

1. Start with **`handoff.md`** — the cover note: bundle index, the paste-ready
   marketplace entry, talking points, and the post-listing README follow-up.
2. Send the curator the four files together:
   - `marketplace-entry.json` — the SHA-pinned marketplace entry to merge.
   - `positioning-brief.md` — 1-page, DQL-led positioning.
   - `validation-proof.md` — captured `claude plugin validate` + smoke-test logs.
3. Once the entry is merged, update the README install line (drop the "once
   listed" caveat) — see `handoff.md` → Post-listing.

Pinned release: `v1.0.0` @ `44a20a931193596243d786ffb02959c8d75a5e8f`.
